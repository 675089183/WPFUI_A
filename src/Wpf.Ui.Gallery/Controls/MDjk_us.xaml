<UserControl x:Class="Wpf.Ui.Gallery.Controls.MDjk_us"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Wpf.Ui.Gallery.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="220">
    <Grid>
        <Border Margin="15,10,0,0" Padding="10" Grid.Column="0" Grid.Row="0" Style="{StaticResource CustomBorder}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Text1}" VerticalAlignment="Center" />
                <ui:ToggleSwitch
            Margin="0,0,5,0"
             Grid.Column="1"
             IsChecked="{Binding IsToggleSwitch}"
                    
             OffContent="Off"
             OnContent="On" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
